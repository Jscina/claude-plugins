# rag-memory commit boundary — what stays local vs. what is committed.
# Generated by /rag:init. Edit to taste; the defaults enforce the documented boundary.

# --- Local working state — never committed ---
issues/backlog/
issues/active/
issues/done/

# --- Trace logs are local working state, even for archived cards ---
**/trace.md

# --- Derived retrieval index (per-user; see the plugin's slot-in-rag feature) ---
.rag-index/

# Committed surface = system/** + issues/archive/** (context.md, benchmarks.md, artifacts/)
