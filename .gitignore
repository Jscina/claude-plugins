# macOS AppleDouble metadata (created when this drive was used on a Mac)
._*
.DS_Store
.AppleDouble
.LSOverride

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
Thumbs.db
desktop.ini

# Logs / temp
*.log
*.tmp

# Built artifacts (decision-engine MCP server is built on first session via hooks/ensure-build.sh)
plugins/decision-engine/mcp-server/dist/
plugins/decision-engine/mcp-server/node_modules/

# Python bytecode (rag plugin ships bin/rag-new-card)
__pycache__/
*.pyc

# Python dev/test tooling (local only; never committed)
.venv/
venv/
.pytest_cache/
