[pytest]
# Dev-only test suite for the marketplace's plugin bin/ scripts. Lives at the repo root,
# outside plugins/, so it is never part of any shipped plugin payload.
testpaths = tests
addopts = -q
